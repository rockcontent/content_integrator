source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

group :test do
  gem "vcr", "~> 4.0.0"
  gem "rspec", "~> 3.8"
  gem "webmock", "~> 3.4", ">= 3.4.2"
end

# Specify your gem's dependencies in content_integrator.gemspec
gemspec
